<UserControl x:Class="Roslynator.VisualStudio.RefactoringsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Roslynator.VisualStudio"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <DataGrid ItemsSource="{Binding Refactorings}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column">
      <DataGrid.Columns>
        <DataGridTextColumn Binding="{Binding Id}" Header="Id" IsReadOnly="True"></DataGridTextColumn>
        <DataGridTextColumn Binding="{Binding Title}" Header="Title" IsReadOnly="True"></DataGridTextColumn>
        <DataGridCheckBoxColumn Binding="{Binding IsEnabled}" Header="Enabled" IsReadOnly="false"></DataGridCheckBoxColumn>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>
